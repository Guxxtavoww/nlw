import React from 'react';

import { GameCardContainer } from './styles';

interface IGameCardProps {
    image: string;
}

const GameCard: React.FC<IGameCardProps> = ({ image }) => {
  return (
    <GameCardContainer>
         
    </GameCardContainer>
  );
};

export default GameCard;