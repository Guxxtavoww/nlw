import React from 'react';
import { View, Text } from 'react-native';

const Slider: React.FC = () => {
    return (
        <View>
            <Text></Text>
        </View>
    );
};

export default Slider;